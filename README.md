# Fine-Tune-XLSR-Wav2Vec2-on-zh-TW-8K-ASR

Requirements
------------

    Python 3.7

Installation
------------

    pip install -r requirements.txt
    apt-get update && apt-get install -y libsndfile1 ffmpeg
